version: "3.8"
services:
  ollama-train:
    build: .
    container_name: ollama-train
    runtime: nvidia
    env_file:
      - .env

    volumes:
      - ./data:/app/data:ro
      - ./output:/app/output
      - .ollama:/root/.ollama
  
    restart: unless-stopped
